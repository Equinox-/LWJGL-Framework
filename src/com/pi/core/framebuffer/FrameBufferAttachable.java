package com.pi.core.framebuffer;

import com.pi.core.util.GLIdentifiable;

public interface FrameBufferAttachable extends GLIdentifiable {
	// Nothing special, just a tagging interface for framebuffer attachable objects.
}
